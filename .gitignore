# Godot 4 generated cache (machine-specific, do not commit)
.godot/

# Generated import sidecars (regenerated on import)
*.import

# Build artifacts
build/
*.apk

# NOTE: export_presets.cfg and project.godot are kept in the repo (needed for CI build)
